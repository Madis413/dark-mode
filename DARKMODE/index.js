function DARKMODE(){
    /*Guardando en una variable a body para utilizarla después */
    const body = document.body;
    /*classList está en todos los elemnentos
    y nos permite accedery manipular clases de CSS*/
    body.classList.toggle("dark-mode");
}

